{
  "name": "identity-api",
  "version": "1.0.0",
  "description": "a basic identity-api",
  "license": "ISC",
  "author": "@eebaez",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=.env ./src/server"
  },
  "dependencies": {
    "argon2": "^0.44.0"
  }
}
